name: Update README Status

on:
  push:
    paths:
      - "data/**"
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  # Disabled since update is handled server-side now
  # update-readme:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #
  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: 3.x
  #
  #     - name: Install dependencies
  #       run: |
  #         pip install pandas pytz
  #
  #     - name: Update README status fields
  #       run: |
  #         python scripts/update_readme_status_fields.py
  #
  #     - name: Set up SSH
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "${{ secrets.XDCINTEL_SSH_KEY }}" > ~/.ssh/id_ed25519
  #         chmod 600 ~/.ssh/id_ed25519
  #         ssh-keyscan github.com >> ~/.ssh/known_hosts
  #
  #     - name: Commit and Push changes
  #       run: |
  #         git config --global user.name 'xdc-intel-bot'
  #         git config --global user.email 'bot@xdcintel.org'
  #         git add .
  #         if git diff --cached --quiet; then
  #           echo "No changes to commit."
  #         else
  #           git commit -m "Auto-update README"
  #           git push git@github.com:MrBlockchain22/xdc-intel-reports.git HEAD:main
  #         fi
